const a = parseInt(prompt(`ingresa el un numero`));
const b = parseInt(prompt(`ingresa el un numero`));
const c = parseInt(prompt(`ingresa el un numero`));
const d = parseInt(prompt(`ingresa el un numero`));
let result = (a + b + c + d) / 4;
alert(`result${result}`);

const Preciohoras = parseInt(prompt(`ingrese la zona de parquego`));
switch (Preciohoras) {
   case value:
      break;

   default:
      break;
}
const cobro = parseInt(prompt(`ingrese las horas `));
let resultado = Preciohoras * cobro;
alert(`precio a pagar ${resultado}`);

const sueldo = parseInt(prompt(`ingrese su sueldo `));
const procentaje = parseFloat(sueldo * 0.15 * 4) * 12;
alert(` tu  ahorro es ${porcentaje}`);
